#include <iostream>
#include <map>
#include <sstream>
#include <string>
#include <algorithm>

extern std::map<std::string, int> variables;

// Guarda variáveis que são strings
extern std::map<std::string, std::string> str_variables; 